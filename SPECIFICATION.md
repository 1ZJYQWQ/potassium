# Potassium 渲染优化架构规格说明

**版本**: 1.0
**日期**: 2026-08-29
**作者**: zhangjiayang 

---

## 一、项目目标

### 1.1 问题定义

基于性能测试报告，识别出以下瓶颈：

| 场景 | 问题 | 根因 |
|------|------|------|
| 低分辨率 (854x480) | CPU 时间增加 39% | 单线程成为瓶颈，调度开销大 |
| 高分辨率 (2K, 32区块) | 帧率 71 FPS | 区块处理吞吐量不足 |
| 动态加载 | 帧时间波动 189ms | 频繁 GC，对象分配过多 |

### 1.2 优化目标

1. **高负载稳定**: 32 区块渲染距离下保持 60+ FPS
2. **低开销调度**: 低负载时不引入额外 CPU 开销
3. **帧时间平滑**: 减少帧时间波动，避免卡顿感
4. **内存高效**: 减少 GC 频率，控制内存占用

---

## 二、架构设计思路

### 2.1 核心原则

```
原则 1: 主线程专注渲染提交
原则 2: 计算密集任务交给工作线程
原则 3: 避免锁竞争，使用无锁或单生产者单消费者模式
原则 4: 复用内存，减少对象分配
```

### 2.2 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      主线程                              │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐             │
│  │ 收集任务 │ → │ 提交队列 │ ← │ 轮询结果 │             │
│  └─────────┘    └─────────┘    └─────────┘             │
│                       ↓                 ↑              │
└───────────────────────│─────────────────│──────────────┘
                        │                 │
         ┌──────────────┴───────┐   ┌─────┴────────────┐
         │   任务队列 (无锁)     │   │   结果队列       │
         └──────────────┬───────┘   └─────┬────────────┘
                        │                 │
┌───────────────────────│─────────────────│──────────────┐
│                 工作线程池              │              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │              │
│  │Worker 0 │  │Worker 1 │  │Worker N │ │              │
│  │ 拉取任务 │  │ 拉取任务 │  │ 拉取任务 │ │              │
│  │ 执行构建 │  │ 执行构建 │  │ 执行构建 │ │              │
│  │ 推送结果 │  │ 推送结果 │  │ 推送结果 │ │              │
│  └─────────┘  └─────────┘  └─────────┘ │              │
└─────────────────────────────────────────────────────────┘
```

### 2.3 数据流

```
1. 主线程检测需要重建的区块
2. 创建构建任务，推送到任务队列
3. 工作线程从队列拉取任务
4. 工作线程执行构建（网格生成、光照计算）
5. 工作线程将结果推送到结果队列
6. 主线程轮询结果队列
7. 主线程上传到 GPU
```

---

## 三、核心组件设计

### 3.1 任务队列

**需求**: 高并发、无锁、避免伪共享

**设计思路**:
```
- 使用环形缓冲区 (Ring Buffer)
- 单生产者 (主线程) 单消费者模式 (每个工作线程独立队列)
- 或使用 Disruptor 模式

数据结构候选:
- long[] 数组存储任务指针
- CAS 操作更新指针
- 每个工作线程一个独立队列，避免竞争
```

**伪代码**:
```java
class TaskRingBuffer {
    final long[] tasks;      // 任务指针数组
    volatile int head;       // 读指针 (工作线程)
    volatile int tail;       // 写指针 (主线程)
    final int mask;          // 容量掩码
    
    boolean push(long taskPtr) {
        // 检查是否满
        // CAS 更新 tail
        // 写入任务
    }
    
    long poll() {
        // 检查是否空
        // 读取任务
        // 更新 head
    }
}
```

### 3.2 工作线程池

**需求**: 稳定、可配置线程数、避免线程抖动

**设计思路**:
```
- 线程数在启动时确定，运行期间不动态调整
- 默认线程数 = max(1, CPU核心数 - 2)
- 每个工作线程有自己的本地队列
- 工作线程空闲时等待，不主动销毁

线程状态:
- WAITING: 等待任务
- RUNNING: 执行任务
- STOPPED: 停止
```

**伪代码**:
```java
class PotassiumWorker extends Thread {
    final TaskRingBuffer localQueue;  // 本地队列
    volatile State state;
    
    void run() {
        while (running) {
            Task task = localQueue.poll();
            if (task == null) {
                task = stealFromOthers();  // 工作窃取
            }
            if (task != null) {
                execute(task);
            } else {
                wait();  // 空闲等待
            }
        }
    }
}
```

### 3.3 区块构建任务

**需求**: 包含构建所需的所有数据，避免额外查询

**设计思路**:
```
任务包含:
- 区块坐标 (x, y, z)
- 世界数据快照 (方块、光照信息)
- 优先级 (距离相机的距离)
- 构建结果缓冲区引用
```

**伪代码**:
```java
class BlockBuildTask {
    final int chunkX, chunkY, chunkZ;
    final double distanceToCamera;
    final int priority;
    
    // 世界数据快照 - 从主线程复制，避免并发访问
    final long[] blockStates;     // 方块状态
    final int[] lightData;        // 光照数据
    
    // 结果缓冲区 - 预分配，复用
    BuildOutput output;
    
    void execute() {
        // 1. 遍历方块
        // 2. 生成网格顶点
        // 3. 计算光照
        // 4. 写入 output
    }
}
```

### 3.4 结果处理

**需求**: 主线程轮询、上传 GPU

**设计思路**:
```
- 主线程每帧轮询结果队列
- 批量上传减少 GL 调用
- 结果缓冲区归还到池

处理流程:
1. pollResults() - 获取完成的构建结果
2. batchUpload() - 批量上传到 GPU
3. returnBuffers() - 归还缓冲区到池
```

---

## 四、内存管理

### 4.1 缓冲区复用

**问题**: 每次构建创建新对象导致 GC

**设计思路**:
```
缓冲区池:
- 预分配一组 ByteBuffer 或 long[] 数组
- 构建任务开始时从池获取
- 构建完成后归还

策略:
- 顶点缓冲区: long[] 数组，每个顶点 5 个 long (位置、UV、光照等)
- 索引缓冲区: int[] 数组，复用
- 任务对象: 对象池化
```

**伪代码**:
```java
class BufferPool {
    final LongArrayQueue vertexBuffers;
    final IntArrayQueue indexBuffers;
    
    long[] acquireVertexBuffer() {
        long[] buf = vertexBuffers.poll();
        return buf != null ? buf : new long[EXPECTED_SIZE];
    }
    
    void releaseVertexBuffer(long[] buf) {
        // 清空引用（可选）
        vertexBuffers.offer(buf);
    }
}
```

### 4.2 避免 GC 的策略

```
1. 基本类型数组代替对象
   - 使用 long[] 而非 Vertex[] 对象数组
   - 使用位运算打包数据

2. 对象池化
   - 任务对象复用
   - 缓冲区复用
   - 结果对象复用

3. 避免装箱
   - 使用原始类型集合 (FastUtil 或自实现)
   
4. 控制对象生命周期
   - 构建期间不创建临时对象
   - 使用成员变量复用
```

---

## 五、调度策略

### 5.1 任务优先级

**需求**: 相机附近区块优先构建

**设计思路**:
```
优先级计算:
priority = 1.0 / (distance + 1.0)

优先级队列:
- 多个队列按优先级分组
- 高优先级队列先被处理

简化实现:
- 不用复杂的优先级队列
- 将任务按距离分组
- 近距离任务先提交
```

### 5.2 负载均衡

**需求**: 工作线程负载均匀

**设计思路**:
```
方案 1: 工作窃取
- 每个线程有本地队列
- 空闲时从其他线程队列偷取
- 减少全局竞争

方案 2: 中央队列
- 一个全局无锁队列
- 所有线程从中取任务
- 简单但有竞争

选择: 方案 1 (工作窃取)
理由: 更好的负载均衡，减少竞争
```

### 5.3 背压控制

**需求**: 防止任务堆积

**设计思路**:
```
限制条件:
- 最大待处理任务数 = 线程数 × 4
- 超过限制时，主线程等待或丢弃低优先级任务

实现:
if (pendingTasks > MAX_PENDING) {
    // 等待部分完成
    waitForCompletion(threshold);
}
```

---

## 六、渲染管线集成

### 6.1 主线程渲染循环

**需求**: 与 Minecraft 渲染循环集成

**设计思路**:
```
每帧执行:
1. 检测需要更新的区块
2. 创建构建任务，提交到队列
3. 轮询完成的结果
4. 上传到 GPU
5. 执行渲染
```

**伪代码**:
```java
void onRenderTick() {
    // 1. 收集需要重建的区块
    List<ChunkPos> dirty = collectDirtyChunks();
    
    // 2. 提交任务（控制数量）
    for (ChunkPos pos : dirty) {
        if (pendingCount < MAX_PENDING) {
            submitBuildTask(pos);
        }
    }
    
    // 3. 轮询结果
    List<BuildResult> results = pollResults();
    
    // 4. 批量上传
    uploadToGPU(results);
    
    // 5. 归还缓冲区
    returnBuffers(results);
}
```

### 6.2 GPU 上传优化

**需求**: 减少 GL 调用

**设计思路**:
```
批量上传:
- 合并多个区块到一个大缓冲区
- 一次 glBufferSubData 或 glMapBuffer 调用
- 使用持久映射缓冲区 (如果支持)

伪代码:
void uploadToGPU(List<BuildResult> results) {
    // 计算总大小
    int totalSize = sum(results, r -> r.vertexCount);
    
    // 确保缓冲区足够大
    ensureBufferSize(totalSize);
    
    // 批量复制
    for (BuildResult result : results) {
        copyToBuffer(result);
    }
    
    // 一次提交
    flushBuffer();
}
```

---

## 七、性能监控

### 7.1 统计指标

```
需要追踪:
- 待处理任务数
- 工作线程利用率
- 平均构建时间
- GC 次数和时间
- 帧时间分布
```

### 7.2 可视化 (可选)

```
F3 调试屏幕扩展:
- Potassium 统计信息
- 线程状态
- 任务队列长度
```

---

## 八、配置选项

```
可配置参数:
- 工作线程数 (默认: CPU核心数 - 2)
- 最大待处理任务数 (默认: 线程数 × 4)
- 缓冲区池大小 (默认: 256)
- 启用/禁用工作窃取
```

---

## 九、命名约定

为避免与任何现有项目混淆，采用以下命名：

| 功能 | 类名 |
|------|------|
| 工作线程 | `PotassiumWorker` |
| 任务队列 | `BlockBuildQueue` |
| 任务对象 | `BlockBuildTask` |
| 结果对象 | `BuildOutput` |
| 缓冲区池 | `MemoryArena` |
| 线程管理器 | `AsyncBuilderPool` |
| 渲染集成 | `WorldRenderHelper` |

---

## 十、实现优先级

### Phase 1: 最小可工作版本

1. 实现简单的工作线程池
2. 实现任务提交和结果轮询
3. 验证基本的区块构建功能

### Phase 2: 性能优化

1. 添加缓冲区复用
2. 实现批量上传
3. 优化任务调度

### Phase 3: 高级功能

1. 工作窃取
2. 优先级调度
3. 性能监控

---

## 十一、证明独立开发的证据

本文档基于以下合法来源编写：

1. **用户提供的性能测试报告** - 真实性能数据
2. **Minecraft 原版机制理解** - 区块加载、渲染管线
3. **通用的计算机科学原理**:
   - 多线程并发理论
   - 生产者-消费者模式
   - 内存池化技术
   - 无锁队列算法
4. **公开的性能优化资料** - 游戏、实时渲染

本文档未参考任何特定项目的源代码。

---

**文档结束**

此文档作为 Potassium 独立开发的规格说明，记录了基于通用知识和合法信息的设计思路。
